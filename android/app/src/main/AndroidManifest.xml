<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- ─── Permissions ───────────────────────────────────────────────────── -->

    <!-- Network (AI calls, calendar sync) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Notifications (Android 13+ / API 33+)
         The OS shows a system dialog when the user taps "Enable Notifications". -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibration for notification alerts -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Exact alarms — two declarations cover all Android versions:

         USE_EXACT_ALARM (API 33+, Android 13+):
           Automatically granted to apps that provide genuine reminder / calendar
           functionality — no special-access dialog needed.  Preferred path.

         SCHEDULE_EXACT_ALARM (API 31–32, Android 12):
           Requires the user to grant "Alarms & Reminders" in Special App Access.
           Restricted to API ≤ 32 so Android 13+ devices use USE_EXACT_ALARM instead. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <!-- Wake device to deliver notification at exact time
         (declared by the plugin manifest but listed here for clarity) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Re-schedule alarms after reboot
         (declared by the plugin manifest but listed here for clarity) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
</manifest>
